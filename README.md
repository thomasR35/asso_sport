# AS Dynamo — Site associatif

Monorepo contenant le frontend et le backend du site de l'AS Dynamo.

## Structure

- `frontend/` — Vite + React + TypeScript
- `backend/` — PHP 8.2 MVC

## Démarrage rapide

Voir les README dans chaque dossier.
